# This file represents the base specification of your challenge. It is used by
# other tools to install and deploy your challenge.

# Required sections
name: "JaiPonG"
author: "underzero"
category: Forensic
description: |
    Your task is pretty straightforward, repair this corrupted image and get the flag.

    Flag format: `n1mdaCTF{flag}`

    **Author**: `underzero`
value: 10
type: standard

# Hints are used to give players a way to buy or have suggestions. They are not
# required but can be nice.
# Can be removed if unused
# Accepts dictionaries or strings
hints:
    - You can use `identify` command with verbose option to check if there're corruption


# Flags specify answers that your challenge use. You should generally provide
# at least one.
# Can be removed if unused
# Accepts strings or dictionaries
flags:
    - n1mdaCTF{never_give_up_and_always_grinding_cd55d3b0}

# Tags are used to classify your challenge with topics. You should provide at
# least one.
# Can be removed if unused
# Accepts strings
tags:
    - File format
    - Exercises

# Provide paths to files from the same directory that this file is in
# Accepts strings
files:
    - dist/challenge.jpg

# The state of the challenge.
# This is visible by default. It takes two values: hidden, visible.
state: visible

# Specifies what version of the challenge specification was used.
# Subject to change until ctfcli v1.0.0
version: "0.1"
